matplotlib>=3.2.2
numpy<2.0.0

# 视觉处理核心
ultralytics>=8.3.1
opencv-python>=4.5.0
opencv-contrib-python==4.8.1.78
faiss-cpu>=1.7.0
lapx>=0.5.2

# 深度学习框架 - 最稳定LTS版本
torch==2.1.2
torchvision==0.16.2
onnxruntime==1.16.1

# 图像处理
pillow==10.3.0

# 几何计算
shapely>=2.0.0

# 系统工具
psutil>=5.9.5
mediapipe>=0.10.3

# Web服务相关
flask>=2.0.1
requests>=2.31.0

# 数据处理
pandas>=2.0.3

# 选择性安装
onnxruntime-gpu  # 如果支持CUDA，获得更好的推理性能
# pip install -r ./requirements.txt -i http://mirrors.aliyun.com/pypi/simple/ --upgrade --default-timeout=100000 --no-cache-dir --trusted-host mirrors.aliyun.com --no-deps
